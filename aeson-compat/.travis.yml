# This file has been generated by travis-meta-yaml 0.2.0.0
# see https://github.com/phadej/travis-meta-yaml
sudo: false
language: c
before_install:
- unset CC
- export PATH=/opt/ghc/$GHCVER/bin:/opt/cabal/$CABALVER/bin:$PATH
- echo "$(ghc --version) [$(ghc --print-project-git-commit-id 2> /dev/null || echo
  '?')]"
- cabal --version
- ghc --version
- if [ "$NOTESTS" = "YES" ]; then export CABALCONFOPTS="$CABALFLAGS"; export CABALTEST="true";
  else export CABALCONFOPTS="$CABALFLAGS --enable-tests --enable-benchmarks"; export
  CABALTEST="cabal test"; fi
- if [ "$PEDANTIC" = "YES" ]; then export CABALBUILDOPTS="--ghc-options=-Werror --ghc-options=-Wall";
  fi
install:
- if [ -f $HOME/.cabal/packages/hackage.haskell.org/00-index.tar.gz ]; then zcat $HOME/.cabal/packages/hackage.haskell.org/00-index.tar.gz
  > $HOME/.cabal/packages/hackage.haskell.org/00-index.tar; ls -l $HOME/.cabal/packages/hackage.haskell.org/;
  fi; cabal update; sed -i 's/^jobs:/-- jobs:/' ${HOME}/.cabal/config; if [ -n "$CABALCONFIG"
  ]; then cp $CABALCONFIG cabal.config; fi; if [ -n "$STACKAGESNAPSHOT" ]; then curl
  --silent https://www.stackage.org/$STACKAGESNAPSHOT/cabal.config | grep -v "$(cabal
  info . -v0 | head -n 1 | awk '{ print $2 }' | sed -E 's/-[0-9]+(\.[0-9]+)+//') =="
  > cabal.config; fi; cabal install --only-dependencies $CABALCONFOPTS --dry -v >
  installplan.txt; sed -i -e '1,/^Resolving /d' installplan.txt; cat installplan.txt;
  if diff -u $HOME/.cabsnap/installplan.txt installplan.txt; then echo "cabal build-cache
  HIT"; rm -rfv .ghc; cp -av $HOME/.cabsnap/ghc $HOME/.ghc; cp -av $HOME/.cabsnap/lib
  $HOME/.cabsnap/share $HOME/.cabsnap/bin $HOME/.cabal/; else echo "cabal build-cache
  MISS"; rm -rf $HOME/.cabsnap; mkdir -p $HOME/.ghc $HOME/.cabal/lib $HOME/.cabal/share
  $HOME/.cabal/bin; cabal install --only-dependencies $CABALCONFOPTS; fi; if [ ! -d
  $HOME/.cabsnap ]; then echo "snapshotting package-db to build-cache"; mkdir $HOME/.cabsnap;
  cp -av $HOME/.ghc $HOME/.cabsnap/ghc; cp -av $HOME/.cabal/lib $HOME/.cabal/share
  $HOME/.cabal/bin installplan.txt $HOME/.cabsnap/; fi;
script:
- if [ -f configure.ac ]; then autoreconf -i; fi
- cabal configure $CABALCONFOPTS -v2
- cabal build $CABALBUILDOPTS
- $CABALTEST
- if [ "$CABALVER" = "1.22" ]; then cabal check; fi
- cabal sdist
- SRC_TGZ=$(cabal info . | awk '{print $2;exit}').tar.gz && (cd dist && cabal install
  --force-reinstalls $CABALFLAGS "$SRC_TGZ")
matrix:
  fast_finish: true
  include:
  - env: GHCVER=7.6.3 CABALVER=1.18
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.18
        - ghc-7.6.3
        - libgmp-dev
    compiler: ! ': # ghc-7.6.3 cabal-1.18'
  - env: GHCVER=7.6.3 CABALVER=1.18 CABALCONFIG=aeson-0.7.0.6.cabal.config
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.18
        - ghc-7.6.3
        - libgmp-dev
    compiler: ! ': # ghc-7.6.3 cabal-1.18  aeson-0.7.0.6.cabal.config'
  - env: GHCVER=7.8.4 CABALVER=1.18
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.18
        - ghc-7.8.4
        - libgmp-dev
    compiler: ! ': # ghc-7.8.4 cabal-1.18'
  - env: GHCVER=7.8.4 CABALVER=1.18 CABALCONFIG=aeson-0.7.0.6.cabal.config
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.18
        - ghc-7.8.4
        - libgmp-dev
    compiler: ! ': # ghc-7.8.4 cabal-1.18  aeson-0.7.0.6.cabal.config'
  - env: GHCVER=7.8.4 CABALVER=1.18 STACKAGESNAPSHOT=lts-2.22
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.18
        - ghc-7.8.4
        - libgmp-dev
    compiler: ! ': # ghc-7.8.4 cabal-1.18 lts-2.22'
  - env: GHCVER=7.10.2 CABALVER=1.22
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-7.10.2
        - libgmp-dev
    compiler: ! ': # ghc-7.10.2 cabal-1.22'
  - env: GHCVER=7.10.2 CABALVER=1.22 STACKAGESNAPSHOT=lts-3.6
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-7.10.2
        - libgmp-dev
    compiler: ! ': # ghc-7.10.2 cabal-1.22 lts-3.6'
  - env: GHCVER=7.10.3 CABALVER=1.22
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-7.10.3
        - libgmp-dev
    compiler: ! ': # ghc-7.10.3 cabal-1.22'
  - env: GHCVER=7.10.3 CABALVER=1.22 STACKAGESNAPSHOT=lts-4.2
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-7.10.3
        - libgmp-dev
    compiler: ! ': # ghc-7.10.3 cabal-1.22 lts-4.2'
  - env: GHCVER=7.10.3 CABALVER=1.22 STACKAGESNAPSHOT=lts-5.2
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-7.10.3
        - libgmp-dev
    compiler: ! ': # ghc-7.10.3 cabal-1.22 lts-5.2'
  - env: GHCVER=7.10.3 CABALVER=1.22 CABALCONFIG=time-1.6.cabal.config NOTESTS=YES
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-7.10.3
        - libgmp-dev
    compiler: ! ': # ghc-7.10.3 cabal-1.22  time-1.6.cabal.config'
  - env: GHCVER=8.0.1 CABALVER=1.24
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.24
        - ghc-8.0.1
        - libgmp-dev
    compiler: ! ': # ghc-8.0.1 cabal-1.24'
  - env: GHCVER=head CABALVER=1.22
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-1.22
        - ghc-head
        - libgmp-dev
    compiler: ! ': # ghc-head cabal-1.22'
  allow_failures:
  - env: GHCVER=head CABALVER=1.22
branches:
  only:
  - master
before_cache:
- rm -fv $HOME/.cabal/packages/hackage.haskell.org/build-reports.log
- rm -fv $HOME/.cabal/packages/hackage.haskell.org/00-index.tar
cache:
  directories:
  - $HOME/.cabsnap
  - $HOME/.cabal/packages
