resolver: lts-14.1
packages:
  - ./persistent
  - ./persistent-template
  - ./persistent-sqlite
  - ./persistent-test
  - ./persistent-mongoDB
  - ./persistent-mysql
  - ./persistent-postgresql
  - ./persistent-redis
  - ./persistent-qq

extra-deps:
  - th-lift-instances-0.1.14

allow-newer: true
nix:
  pure: true
  enable: true
  packages: [ zlib postgresql mysql80 pcre mysql-client zstd openssl ]